spring:
#  추가된 부분
  mail:
    host: smtp.gmail.com
    port: 587
    username: hada3817@gmail.com
    password: appkey
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:mysql://localhost:3306/GoodOrBad?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: /
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

  server:
    servlet:
      session:
        timeout: 1h


